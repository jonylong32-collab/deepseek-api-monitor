# DeepSeek API Monitor Frontend

React + TypeScript + Vite frontend for DeepSeek API Monitor.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run lint
```

The development server proxies `/api` requests to `http://127.0.0.1:8000`.
